Inspect the repository and make the smallest safe change that folds a fresh duplicate helper or branch back into its existing owner.

Prefer one obvious owner path and remove the new duplicate instead of leaving two near-identical implementations behind.

If no high-confidence duplicate cleanup is available, say so and pick the next highest-confidence clone or helper-consolidation target.
