Inspect the repository's calibration and telemetry code and make the smallest safe change that improves repo-local session telemetry clarity or correctness.

Prefer a narrow fix that keeps the eval loop easy to read and reason about.

If the current implementation already looks sound, say so and pick the next highest-confidence maintenance target.
