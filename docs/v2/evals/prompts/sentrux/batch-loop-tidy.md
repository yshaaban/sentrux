Inspect the repository and make the smallest safe change that keeps the batch calibration loop tidy and maintainable.

Prefer an explicit change that reduces duplication or ambiguity in the repo-level calibration assets.

If the loop already looks clean, say so and pick the next highest-confidence maintenance target.
