Inspect the repository and make the smallest safe change that collapses a fresh duplicate implementation back into its existing owner.

Prefer reusing or extending the current shared helper over introducing a second path that can drift.

If no high-confidence duplicate cleanup is available, say so and pick the next highest-confidence clone or helper-consolidation target.
